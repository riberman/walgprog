<% provide(:head_title, t('contacts.public.new.title')) %>
<% provide(:page_title, t('contacts.public.new.title')) %>

<%= render 'form', path: contacts_path, button_name: t('contacts.public.new.button') %>

