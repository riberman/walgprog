<tr>
  <td><%= institution.name %></td>
  <td><%= institution.acronym %></td>
  <td class="text-nowrap"><%= institution.city.name %></td>
  <td class="text-nowrap"><%= institution.city.state.acronym %></td>
  <td>
    <div class="btn-group">
      <%= link_to edit_admins_institution_path(institution), class: 'icon ml-2', title: t('helpers.edit') do %>
        <%= icon('fas', 'edit') %>
      <% end %>

      <%= link_to admins_institution_path(institution),class: 'icon ml-2 destroy', method: :delete,
                  data: {confirm: t('prompt.confirm')}, title: t('helpers.delete') do %>
        <%= icon('fas', 'trash') %>
      <% end %>
    </div>
  </td>
</tr>