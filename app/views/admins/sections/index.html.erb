<% provide(:head_title, t('sections.index')) %>
<% provide(:page_title, t('sections.index')) %>

<div class="row">
  <div class="col-sm-12 col-lg-4 offset-lg-8">
    <%= link_to new_admins_event_section_path, class: 'btn btn-outline-primary d-block' do %>
      <i class="fe fe-plus mr-2"></i>
      <%= t('events.new') %>
    <% end %>
  </div>
</div>

<div class="table-striped mt-3 table-responsive w-100">
  <table class="table table-hover table-outline table-vcenter card-table">
    <thead>
      <tr>
        <th><%= t('activerecord.attributes.attribute_commons.title') %></th>
        <th><%= t('activerecord.attributes.section.content') %></th>
        <th><%= t('activerecord.attributes.section.status') %></th>
        <th><%= t('activerecord.attributes.section.alternative_text') %></th>
        <th><%= t('activerecord.attributes.section.index') %></th>
        <th><%= t('activerecord.attributes.section.icon') %></th>
        <th><%= t('activerecord.models.event.one') %></th>
        <th><%= t('activerecord.attributes.attribute_commons.created_at') %></th>
        <th></th>
      </tr>
    </thead>
    <tbody>
      <%= render @sections %>
    </tbody>
  </table>
</div>
